@isTest(SeeAllData=true)
public class studentCounterTest {
    @isTest static void TestCounter() 
    {
        ID classID = 'a062w000000I46dAAC';
        Integer initialCount = (Integer)[SELECT Mycount__c FROM Class__c WHERE ID = :classID][0].Mycount__c;
        Student__c st = new Student__c();
        st.Name = 'Rahul';
        st.LastName__c = 'Sharma';
        st.class__c = 'a062w000000I46dAAC';
        Test.startTest();
        insert st;    
        Test.stopTest();
        Integer afterCount = (Integer)[SELECT Mycount__c FROM Class__c WHERE ID = :classID][0].Mycount__c;
        system.assertEquals(initialCount, afterCount - 1);
       
        
    }
    
}