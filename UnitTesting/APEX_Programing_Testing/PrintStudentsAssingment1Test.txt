@isTest
public class PrintStudentsAssingment1Test 
{
    @testSetup 
    static void setupDataForClassStudent() 
    {
        Class__c cls = new Class__c(Name = 'TestClass', MaxSize__c = 3, MyCount__c = 0);
        insert cls;
        List<Student__c> st = new List<Student__c>();
        for(Integer count = 0;count < 2;count++) 
        {
            st.add(new Student__c(Name = 'TestStudent' + count, LastName__c = 'Lastname' + count, Class__c = cls.Id));
        }
        insert st;
    }
    @isTest 
    static void testStudentList_matchStudentList_getSuccessRespond() 
    {
        Id classId = [SELECT Id from Class__c WHERE Name = 'TestClass'].Id;
        Test.startTest();
        integer actualStudentListCount = PrintStudentsAssingment1.GetAllStudents(classId).size();
        Test.stopTest();
        System.assertEquals(2, actualStudentListCount);	
    }
}
