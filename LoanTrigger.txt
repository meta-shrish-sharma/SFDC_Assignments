trigger LoanTrigger on Loan__c (before insert) 
{
    Map<id, Loan__c> updatedLoanList = new Map<id, Loan__c>([SELECT Name, Manager__c, City__r.Name, City__r.Manager__c FROM Loan__c WHERE Id IN :trigger.new]);
    for( Loan__c insLoans: trigger.new)
    {
        if(insLoans.Manager__c == null && updatedLoanList.get(insLoans.Id) != null)
        {
            insLoans.Manager__c = updatedLoanList.get(insLoans.Id).City__r.Manager__c;
        }
    }
}
